tests:
  - name: 'with timeouted command'
    command:
      - type: env
        name: TEXEST
      - "-"
    stdin:
      format: yaml
      value:
        tests:
          - command:
              - 'sleep'
              - '2'
            timeout: 0
    expect:
      status:
        eq: 1
