tests:
  - name: 'with timeouted command'
    command:
      - $env: TEXEST
      - "-"
    stdin:
      $yaml:
        tests:
          - command:
              - 'sleep'
              - '2'
            timeout: 0
    expect:
      status:
        eq: 1
